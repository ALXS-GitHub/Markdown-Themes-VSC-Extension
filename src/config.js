const HOST = 'localhost';
const PORT = 3456;
const SERVER_URL = "http://" + HOST + ":" + PORT;
const THEMES_URL = SERVER_URL + "/themes";

module.exports = {
    HOST,
    PORT,
    SERVER_URL,
    THEMES_URL
};